## 0.2.1

- Automatically resolve `ResolvableResponse` in `IonTestClient`.
- Add `readText()` helper on `Response`.
- Make reading streamed response bodies idempotent for repeated reads.

## 0.2.0

- Upgraded `ion_extra` to `^0.2.0`.

## 0.1.0

- Initial version.
