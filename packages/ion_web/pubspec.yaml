name: ion_web
description: HTTP/1.1 web server.
homepage: https://github.com/iv-re/ion
repository: https://github.com/iv-re/ion/tree/main/packages/ion_web
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  ctx: ^1.0.0
  equatable: ^2.1.0
  http_headers: ^0.1.0
  http_sse: ^0.1.0
  meta: ^1.12.0
  mime: ^2.0.0
  sl: ^1.1.0
  stream_channel: ^2.1.2
  stream_web_socket_channel: ^0.1.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  mocktail: ^1.0.5
  test: ^1.25.6

false_secrets:
  - /test/helpers/tls_fixtures.dart